package com.uprojects.launcher;

import com.uprojects.server.GameServer;

import java.io.IOException;

public class ServidorLauncher {

    /*
    public static void main(String[] args) {

        try {
            new GameServer();
        } catch (IOException e) {
            System.err.println("Error: Could not start server. Is the port 54555 already in use?");
            e.printStackTrace();
        }
    }

     */
}
